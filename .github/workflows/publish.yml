name: Publish to (Test-)PyPI

on:
  workflow_dispatch:
    inputs:
      target:
        description: "Publish target"
        required: true
        default: testpypi
        type: choice
        options:
          - testpypi
          - pypi
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.pkg }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pkg: [jetson-cli, jetson-containers]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tooling
        run: python -m pip install --upgrade build twine

      - name: Rewrite pyproject name + readme for ${{ matrix.pkg }}
        env:
          PKG: ${{ matrix.pkg }}
        run: |
          python - <<'PY'
          import os
          import pathlib
          import re

          pkg = os.environ["PKG"]
          path = pathlib.Path("pyproject.toml")
          text = path.read_text()
          text = re.sub(r'(?m)^name = ".*"', f'name = "{pkg}"', text, count=1)
          text = re.sub(
              r'(?m)^readme = ".*"',
              f'readme = "packaging/README.{pkg}.md"',
              text,
              count=1,
          )
          path.write_text(text)
          print(f"Rewrote pyproject.toml for {pkg}")
          PY

      - name: Build sdist + wheel
        run: python -m build

      - name: Verify distribution metadata
        run: twine check dist/*

      - uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.pkg }}
          path: dist/

  publish-testpypi:
    name: Publish ${{ matrix.pkg }} to Test-PyPI
    if: github.event_name == 'workflow_dispatch' && inputs.target == 'testpypi'
    needs: build
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        pkg: [jetson-cli, jetson-containers]
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist-${{ matrix.pkg }}
          path: dist
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  publish-pypi:
    name: Publish ${{ matrix.pkg }} to PyPI
    if: >
      (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')) ||
      (github.event_name == 'workflow_dispatch' && inputs.target == 'pypi')
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        pkg: [jetson-cli, jetson-containers]
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist-${{ matrix.pkg }}
          path: dist
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
